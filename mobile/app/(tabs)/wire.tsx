import React from 'react';
import Placeholder from '../../components/Placeholder';

export default function WireScreen() {
  return (
    <Placeholder
      title="The Wire"
      note="The discovery feed — hunt finds, heat-ranked. Coming over from /api/wire."
    />
  );
}
