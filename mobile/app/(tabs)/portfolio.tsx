import React from 'react';
import Placeholder from '../../components/Placeholder';

export default function PortfolioScreen() {
  return (
    <Placeholder
      title="Portfolio"
      note="Positions, NAV, and the Tape will live here — same /api/portfolio feed the native app used."
    />
  );
}
