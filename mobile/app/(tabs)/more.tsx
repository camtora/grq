import React from 'react';
import Placeholder from '../../components/Placeholder';

export default function MoreScreen() {
  return (
    <Placeholder
      title="More"
      note="Settings, the kill switch, FX approvals, Smart Money, the labs — this tab gets built out shortly. Search moved to the floating button, bottom-right."
    />
  );
}
