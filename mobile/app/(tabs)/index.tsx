import React from 'react';
import Placeholder from '../../components/Placeholder';

export default function TodayScreen() {
  return (
    <Placeholder
      title="Today"
      note="The Daily lands here next — headlines, the market brief, earnings, and Alfred's read. This is the scaffold: splash works, backend wired, content to come."
    />
  );
}
