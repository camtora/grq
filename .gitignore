.env
.env.*
node_modules/
.next/
*.log
.DS_Store
._*
screenshots/
*.tsbuildinfo

# APNs auth keys — secret, never commit (D53)
*.p8
AuthKey_*.p8

# Xcode build output — TestFlight .xcarchive exports live here (large, regenerated)
archives/
*.xcarchive/

# /brag output — rendered video, hyperframes project, node_modules, snapshots (all regenerable)
brag-output/
brag-output-*/
